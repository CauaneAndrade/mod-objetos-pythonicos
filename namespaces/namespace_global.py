# Namespaces/contextos
# Como o Python evita o conflito de nomes com os chamados Namespaces

print(globals())  # Return the dictionary containing the current scope's global variables.
# o escopo global do Python é a nível de pacote 